/**
 * Setup the lights being used within this scene
 */
define(["babylon", "scene", "geometry", "material", "texture"], function (BABYLON, scene) {
    var assetsManager = new BABYLON.AssetsManager(scene);

    return assetsManager
});